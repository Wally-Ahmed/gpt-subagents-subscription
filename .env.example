# Optional model overrides. Tokens are NOT stored here — they live in
# ~/.gpt-subagents-subscription/auth.json after you run `npm run login`.

# GSS_CODEX_MODEL=gpt-5.3-codex
# GSS_ARCHITECT_MODEL=gpt-5.4
