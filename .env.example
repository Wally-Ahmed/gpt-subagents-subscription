# This MCP stores nothing secret here. Your ChatGPT OAuth tokens live in
# ~/.gpt-subagents-subscription/auth.json after you run `npm run login`.
#
# No env vars are required. The model is chosen per call via the ask_gpt tool's
# required `model` argument (gpt-5.4, gpt-5.4-mini, or gpt-5.5).
